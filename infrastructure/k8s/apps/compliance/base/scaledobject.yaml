apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: compliance-so
spec:
  scaleTargetRef:
    name: compliance
  minReplicaCount: 1
  maxReplicaCount: 4
  triggers:
    - type: cpu
      metadata:
        type: Utilization
        value: "80"
