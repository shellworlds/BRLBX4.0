apiVersion: apps/v1
kind: Deployment
metadata:
  name: iot-ingestion
  labels:
    app.kubernetes.io/name: iot-ingestion
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: iot-ingestion
  template:
    metadata:
      labels:
        app.kubernetes.io/name: iot-ingestion
    spec:
      containers:
        - name: iot-ingestion
          image: iot-ingestion:latest
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: SERVICE_ROOT
              value: /app
            - name: MQTT_BROKER_URL
              valueFrom:
                configMapKeyRef:
                  name: iot-ingestion-config
                  key: mqtt_broker_url
                  optional: true
          envFrom:
            - secretRef:
                name: iot-ingestion-secrets
                optional: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
        - name: cloud-sql-proxy
          image: gcr.io/cloudsql-docker/gce-proxy:1.37.2
          command:
            - /bin/sh
            - -c
          args:
            - exec /cloud_sql_proxy -ip_address_types=PRIVATE -instances="$CLOUDSQL_INSTANCES"
          env:
            - name: CLOUDSQL_INSTANCES
              valueFrom:
                configMapKeyRef:
                  name: iot-ingestion-config
                  key: cloudsql_instances
                  optional: true
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: "300m"
              memory: 256Mi
