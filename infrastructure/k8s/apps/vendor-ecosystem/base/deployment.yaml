apiVersion: apps/v1
kind: Deployment
metadata:
  name: vendor-ecosystem
  labels:
    app.kubernetes.io/name: vendor-ecosystem
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: vendor-ecosystem
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vendor-ecosystem
    spec:
      containers:
        - name: vendor-ecosystem
          image: vendor-ecosystem:latest
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: SERVICE_ROOT
              value: /app
          envFrom:
            - configMapRef:
                name: vendor-ecosystem-config
                optional: true
            - secretRef:
                name: vendor-ecosystem-secrets
                optional: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
        - name: cloud-sql-proxy
          image: gcr.io/cloudsql-docker/gce-proxy:1.37.2
          command:
            - /bin/sh
            - -c
          args:
            - exec /cloud_sql_proxy -ip_address_types=PRIVATE -instances="$CLOUDSQL_INSTANCES"
          env:
            - name: CLOUDSQL_INSTANCES
              valueFrom:
                configMapKeyRef:
                  name: vendor-ecosystem-config
                  key: cloudsql_instances
                  optional: true
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: "300m"
              memory: 256Mi
