apiVersion: batch/v1
kind: CronJob
metadata:
  name: vendor-daily-aggregate
spec:
  schedule: "17 1 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: curl
              image: curlimages/curl:8.6.0
              command:
                - /bin/sh
                - -ceu
                - |
                  curl -f -X POST \
                    -H "X-Internal-Token: ${INTERNAL_AGGREGATE_TOKEN}" \
                    "http://vendor-ecosystem:8080/api/v1/internal/aggregate/daily"
              env:
                - name: INTERNAL_AGGREGATE_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: vendor-ecosystem-secrets
                      key: INTERNAL_AGGREGATE_TOKEN
                      optional: true
