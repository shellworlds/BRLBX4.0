apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: vendor-ecosystem-so
spec:
  scaleTargetRef:
    name: vendor-ecosystem
  minReplicaCount: 1
  maxReplicaCount: 5
  triggers:
    - type: cpu
      metadata:
        type: Utilization
        value: "75"
