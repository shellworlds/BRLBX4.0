# Example workflow — do not apply this file as-is.
#
# kubectl -n dev create secret generic db-credentials \
#   --from-literal=username=backend_user \
#   --from-literal=password=REPLACE_ME \
#   --dry-run=client -o yaml | kubeseal -o yaml > sealed-db-credentials.yaml
#
# Commit sealed-db-credentials.yaml after replacing placeholders with real sealed output.
